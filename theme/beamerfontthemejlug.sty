% Set font style
\usepackage{fontspec}
\setmainfont{Calibri}
% use main font for base text
\usefonttheme{default}

% font for base text
\setmainfont{Calibri}

\setbeamerfont{author}{size=\footnotesize}

% for frames
\setbeamerfont{frametitle}{family=\fontspec{Calibri}, size=\Huge}

% font for title
\setbeamerfont{title}{family=\fontspec{Calibri Bold}}

% for other elements on title page (author, date)
\setbeamerfont{title page}{family=\fontspec{Calibri}}
